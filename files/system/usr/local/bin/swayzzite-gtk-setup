#!/usr/bin/env bash
# Wrapper script that calls the user setup script
set -oue pipefail

SCRIPT_PATH="/usr/local/lib/swayzzite/setup-gtk-themes.sh"

if [ -f "$SCRIPT_PATH" ]; then
    exec "$SCRIPT_PATH"
else
    logger "Swayzzite: GTK setup script not found at $SCRIPT_PATH"
    exit 1
fi
